file: ./movielist-test.csv
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: "jdbc:derby:local;create=true"
    username: "derbyusertest"
    password: "password"
  jpa:
    properties:
      hibernate:
        dialect : org.hibernate.dialect.DerbyDialect